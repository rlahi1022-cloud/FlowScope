#pragma once

// ------------------------------------------------
// packet.h
// 서버 / 클라이언트 공통 네트워크 상수 정의
//
// 책임 범위:
//   - TCP 패킷 프레이밍 상수만 정의
//   - 서버/클라 모두 동일한 값을 사용해야 통신이 성립한다
//
// include 위치:
//   서버 : epollserver, session 등 I/O 계층
//   클라 : clientsocket 등 송수신 계층
//
// 관련 파일:
//   프로토콜 enum       -> common/protocol.h
//   서버 패킷 클래스    -> server/common/packetclass.h
//   클라 패킷 클래스    -> client/common/packetclass.h
// ------------------------------------------------

// ------------------------------------------------
// HEADER_SIZE
// TCP 스트림 선두에 붙는 고정 헤더 크기 (4바이트)
// 빅엔디언 uint32_t 로 바디 길이를 전달한다
// ------------------------------------------------
static constexpr int HEADER_SIZE = 4;

// ------------------------------------------------
// MAX_BODY_SIZE
// 단일 패킷 바디의 최대 허용 크기 (1MB)
// 초과 시 해당 연결을 강제 종료한다
// ------------------------------------------------
static constexpr int MAX_BODY_SIZE = 1024 * 1024;